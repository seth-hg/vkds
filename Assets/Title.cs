using UnityEngine;
using System.Collections;

public class Title : MonoBehaviour {

    //private float scale = 0;

    //void Start()
    //{
    //    transform.localPosition =  new Vector3(scale, scale, scale);
    //}

    //void Update()
    //{
    //    if (scale <= 1)
    //        scale += 0.1f;
    //    transform.localScale = new Vector3(scale, scale, scale);
    //}
}
